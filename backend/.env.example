# Grok/xAI key (preferred)
GROK_API_KEY=

# Optional override if you use another OpenAI-compatible provider
# LLM_API_URL=https://api.x.ai/v1/chat/completions
# LLM_MODEL=grok-2-latest

# Backward-compatible fallback
OPENAI_API_KEY=

GITHUB_TOKEN=
GITHUB_WEBHOOK_SECRET=

# SMTP for OTP emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=no-reply@code-guardian.local
